<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:base="clr-namespace:MafiatorApp.ViewModels.Base;assembly=MafiatorApp"
             x:Class="MafiatorApp.Views.RoomGamesView"
             BackgroundColor="{StaticResource LightBackgroundColor}"
             base:ViewModelLocator.AutoWireViewModel="True">
    <ContentPage.Content>
        <StackLayout IsVisible="{Binding NoGame}" Margin="8">
            <Image Source="nogame.png"></Image>
            <Label HorizontalOptions="CenterAndExpand" Text="بازی مورد انتظاری وجود ندارد"></Label>
            <Button Text="ساخت بازی جدید" Command="{Binding AddGameCommand}" ></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>